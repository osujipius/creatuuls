/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "9bwnz1qwxoaomcjk.public.blob.vercel-storage.com"], // <== Domain name
  },
};

module.exports = nextConfig;
